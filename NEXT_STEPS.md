# Tideway — Build Brief & Work Queue (for the coding agent)

**Audience:** the coding agent (Codex) working in this repo (`tideway-marketplace/`).
**Companion audit:** `../TIDEWAY_AUDIT_2026-07-15.md` (full findings, evidence, line refs). Finding IDs below (F1–F21) map to that audit.
**Last updated:** 17 July 2026.

**Public brand decision:** the customer-facing company and product name is now **Homle**, with the intended public hostname `homle.co.uk`. Visible website, account, booking, payment, operations and notification copy plus the manifest and H-mark logo have been updated. Stable internal `tideway` database schemas, environment variables, storage metadata and migration identifiers deliberately remain unchanged to avoid an unnecessary security/data migration. Public deployment still requires the production runtime gate and verified hosting configuration; the existing site must not be replaced by a static shell whose accounts and bookings cannot work.

**Hosting direction — 17 July 2026:** the owner cancelled Hostinger and selected Render as the intended full-stack host. Homle is a persistent Node/PostgreSQL application with a separate background worker, not a Next.js/Supabase site. The owner explicitly synced the safe root `render.yaml`, creating one free, read-only Docker preview service with automatic deploys off and no database, worker, disk or domain. Its first deployment of `43b70ca` exited because `APP_ORIGIN` was not persisted by the initial Blueprint form; the prepared fix now pins the exact assigned preview origin in source. This is not the full marketplace: Render's free filesystem is temporary and background workers are paid, so real intake stays closed. The previous Hostinger package remains historical rollback evidence only. No database, domain record or paid resource has been created.

**Private GitHub handoff complete — 17 July 2026:** the full verified `main` history is uploaded to the private `markasdubauskasmd-lang/homle-marketplace` repository. Local and remote `main` matched at commit `13e4dc5e3e4a7ebef930a72298d3b28000c2e47e` immediately after upload. `pnpm run prepush:safety` refuses a dirty history, tracked environment/customer/database/key/log/backup/archive files, high-confidence live secrets, missing ignore coverage, oversized files or an oversized tracked repository before publication. No public repository or deployment was created.

**Live Hostinger audit — 16 July 2026:** the new Homle Node application is publicly reachable at `https://homle.co.uk`; both current apex origins returned the cleaning homepage, `/api/health` returned a healthy writable concierge pilot and `/brief` allowed same-origin camera/microphone access. Accounts are not broken by the visible page: the deployment is intentionally running with `marketplace.enabled=false`, so the capability endpoint reports email, Google and Facebook disabled and both social start routes remain closed. Managed PostgreSQL/migrations, SMTP, private object storage, monitoring and approved OAuth credentials must pass staging before that flag can safely change. No live configuration was mutated during this audit.

**Next Hostinger update prepared, not yet published:** verified runtime commit `cbae212a` is packaged as `../Homle-Hostinger-Node-release-cbae212a.zip` (246 ZIP entries / 237 files / 9 directories, 685,408 bytes, SHA-256 `6600125EA2490AC06A22C2307BD70D066C4B8818D488DF6BAA1004A62ED270E5`) with its evidence manifest at `../Homle-Hostinger-Node-release-cbae212a.manifest.json`. It includes all 44 locked migrations, the protected one-action Landlord property editor, authoritative payment window, calm and urgent Landlord payment follow-up, a payment-ready reminder for both participants inside 24 hours and a Cleaner journey prompt inside two hours, while excluding secrets, customer/private data, tests, documentation, Git history and the local tracking lab. Full syntax, database/dependency asset and product test gates pass. No live file or configuration was changed during this update. The pre-Homle public rollback remains `../homle.co.uk-public-rollback-2026-07-16.zip` (SHA-256 `ACCAE642DF16D00FC38BA0C242846D4220EC383B5BCC3B6B4E9984B661191DD1`). See `docs/HOSTINGER_DEPLOYMENT.md`.

## Implementation progress — 17 July 2026

- **Database traffic is now prepared for horizontal scaling:** normal account, search and booking queries continue through bounded `DATABASE_URL` pooling, while production marketplace mode requires a separate direct `REALTIME_DATABASE_URL` to the same database. Each web instance reserves at most one direct session for PostgreSQL booking-event `LISTEN/NOTIFY`; startup proves the restricted role, PostgreSQL version, matching database and `LISTEN/UNLISTEN` support before serving accounts. Failure and shutdown close both pools exactly once. Managed staging now validates both TLS URLs and rejects cross-database wiring. Focused configuration, attachment, readiness and staging tests pass locally; provider connection limits and a two-instance load/reconnect rehearsal remain required before activation.
- **Render client identity is now deployment-safe instead of guessed:** the trusted-client resolver has a dedicated Render mode bound to `NODE_ENV=production` and Render's platform-provided service identity. It validates every IP in Render's bounded forwarding chain and uses Render's documented first client address, while rejecting hand-set provider mode, generic CIDRs, malformed chains and non-Render environments. Existing direct and explicit-CIDR proxy modes remain unchanged. Production preflight and focused spoofing/configuration tests pass; a real two-instance Render rehearsal still remains required before marketplace activation.
- **Render container releases now carry verifiable build identity:** the Docker build consumes Render's platform-provided exact Git commit, validates the ordered migration lock and writes one no-overwrite `homle-release.json` into both web and worker images. Missing, shortened or malformed commits, invalid/duplicate migration evidence and an existing identity fail closed. The runtime can therefore prove that the deployed source and worker match `TIDEWAY_EXPECT_RELEASE` instead of rejecting an unidentified container after launch. Focused generator and Docker allowlist tests pass; a real Linux image build and scan remain pending the private GitHub push and approved Render setup.
- **Free Render preview can no longer lose real enquiries silently:** `PILOT_INTAKE_ENABLED` is now explicit in production. The prepared free Blueprint keeps it false and the server rejects every legacy write with a clear HTTP 503 before touching temporary storage, while health truthfully reports the intake state. Production preflight rejects missing values and any attempt to enable both the file-backed pilot and PostgreSQL marketplace. Focused process and Blueprint regressions cover the boundary. The owner explicitly created the free service; the first deploy failed closed before opening a port because the required origin was absent, and the exact-origin fix is prepared for the next manual sync.
- **Protected property updates now have a one-action Landlord UI:** each saved property exposes one clear mobile-sized action that reuses the existing short property form and owner-authorised `PUT` route. A Landlord can add or correct access, parking, preferences, checklist and address details without recreating the property or exposing them to unrelated users. Replacement access instructions are encrypted before persistence; unchanged addresses retain existing map coordinates while an address change clears stale coordinates for later approved geocoding. Property and room-scan unsaved state are tracked separately, successful edits receive an accessible confirmation, and focused ownership/encryption/mobile tests plus the complete syntax and product suites pass. The marketplace attachment remains disabled pending managed staging, so no real property was changed.
- **Live release identity is now verifiable instead of guessed:** every Hostinger ZIP embeds one bounded `homle-release.json` generated from the exact committed source, build time and locked migration count. Startup rejects malformed or tampered identity data, `/api/health` exposes only those non-secret fields, and the read-only domain verifier requires a packaged identity and can match the exact eight-character manifest commit through `TIDEWAY_EXPECT_RELEASE`. Focused tests cover missing source-tree fallback, malformed/oversized data, ZIP extraction/content, health projection and exact external matching. A new release must be committed and packaged before this can be proven on the live host; no live configuration changed.
- **Account activation is now bound to that exact release too:** `pnpm run preflight:authentication` loads the identity embedded in the running package and requires it to equal `TIDEWAY_EXPECT_RELEASE` from the adjacent private manifest before Google or Facebook configuration can be declared ready. Its report exposes only expected/running commit IDs, booleans, callback URLs and remaining evidence; missing, malformed and mismatched release tests fail closed. This prevents valid credentials from accidentally enabling accounts on an older upload.
- **Background booking work is release-bound as well:** the separate worker loads the same embedded identity and refuses to start unless it exactly matches `TIDEWAY_EXPECT_RELEASE`, before opening monitoring, PostgreSQL, SMTP or storage. Its ready line and private snapshot expose only the source commit and migration count. Missing/malformed/mismatched package tests now protect notification, media-cleanup and consent-bound dispatch jobs from running with older code than the web process.
- **Guarded fresh-staging database bootstrap complete:** one explicit command now checks the locked 45-migration asset set, exact typed confirmation, PostgreSQL 16+, a `_tideway_staging`/`_homle_staging` database name, a separate non-privileged migration owner, both restricted login roles and an empty application schema before changing anything. It then applies every locked migration and both grant files in order and requires the read-only deployment verifier to pass. Credentials stay out of process arguments; the runner stops on the first failure and never drops or cleans a database. Focused regression coverage proves production/role/non-empty/lock denial, exact ordering, secret redaction and stop-on-failure. It remains unexecuted against managed staging pending an approved empty target.
- **Guarded managed-staging service probe complete:** `pnpm run preflight:staging-services` composes the real database, SMTP, private bucket, monitoring and authentication attachment without opening a port or writing business data. It requires an exact confirmation, production TLS, a remote `_tideway_staging`/`_homle_staging` database authenticated only as `tideway_app`, complete secrets and explicitly disabled payments. It performs a read-only schema/role query, SMTP authentication, `HeadBucket` and adapter composition; it sends no email, uploads no object and contacts no OAuth or payment provider. The secret-safe result names the four remaining synthetic staging proofs. Failure/close and release-inclusion tests pass locally; it remains unexecuted until approved managed service credentials exist.
- **Synthetic email/media/monitoring staging evidence is safely executable:** `pnpm run evidence:staging-services` reuses every staging-service guard and adds an exact confirmation bound to a founder-controlled non-customer mailbox whose local part contains `homle-staging`. It sends exactly two auth emails, uploads one embedded 68-byte PNG, verifies the signed upload, sanitizes it to JPEG, verifies a private read, delivers one privacy-minimal synthetic alert, attempts deletion of both random object keys on every outcome and closes all adapters. It creates no account, booking, database row or payment and contacts no OAuth/payment provider. Mock end-to-end tests prove success, payment isolation, cleanup after a failed upload and deterministic closure. It has not been run externally because no approved mailbox or managed service credentials were supplied.
- **Facebook data-deletion callback complete in detached source:** Meta's form-encoded `signed_request` is bounded and HMAC-SHA256 verified in constant time; the raw app-scoped subject is never stored. A function-only authentication transaction creates or reuses one honest account-deletion queue item, unknown subjects complete without account creation, two shared rate-limit scopes protect callback/status reads, and the public confirmation page removes its opaque fragment code before a no-store header-based lookup. Migration 038, runtime grants, readiness probe, exact Hostinger/Meta URLs and focused crypto/HTTP/database-asset tests are included. It remains closed with the marketplace flag and still requires managed PostgreSQL execution, real Meta app registration, privacy fulfilment ownership and non-customer staging proof.
- **Hostinger release boundary now carries the database upgrade path:** release selection includes all locked migrations, the migration lock and both least-privilege grant scripts; packaging re-verifies their order/checksums before emitting the ZIP and records the migration count in its secret-free manifest. This prevents a source upload containing new marketplace code while omitting the matching database change. Applying migrations still requires the separate approved migration-owner workflow; the Node process remains restricted to `tideway_app`.

- **P0-1, P0-2, P0-4 and P0-8 complete** in `719cc24`: room scans require their private tracker token and never return it; browser mutations fail closed without same-origin evidence; `/admin` and `/admin.html` share the same gate; `ADMIN_REQUIRE_KEY=true` closes the localhost exception; isolated HTTP regressions cover each boundary.
- **P0-3, P0-5 and P0-6 complete** in `5cc8f99`: shipped mojibake is repaired and guarded; cloud-synchronised private data paths produce a startup privacy warning; retention remains launch-gated; private-data backups default off-project, reject common cloud-sync destinations, extract and compare every file, and state truthfully that the zip is unencrypted.
- **P0-7 complete in launch documentation**: the Polsia and independent-local scorecards are separated, the current inventory is 197 JavaScript modules / 68 test files, and the unsupported £20 referral claim is inventoried.
- Current full evidence after the first-Administrator provisioning checkpoint passes: 186 syntax/encoding/database/dependency-asset commands and 70 test commands (256 total); the production dependency audit reports no known vulnerabilities and real lead/scan data remains untouched. The earlier verified source archive remains the tracked `5cc8f99` checkpoint: `../Tideway-independent-source-2026-07-16.zip`, 192 entries, SHA-256 `237D331367EF0B0C85AE5420B8FBDB1E405FEFE4BDDCB8ACB84CFAFDFE18E705`.
- **Founder action still required before real intake:** configure an access-restricted off-OneDrive `DATA_DIR`, stop the server and create/store one private backup on an approved encrypted device or vault, then record the retention decisions. The agent must not move or copy live private records without that explicit location/approval.
- **Runtime direction recorded by product requirements:** PostgreSQL is the production marketplace path required by the requested Uber-style product; the NDJSON application remains a time-boxed Tier 0 concierge pilot. Do not add long-term auth or payments to the NDJSON monolith. The next engineering gate is managed staging attachment (database, SMTP and private bucket), not another local data model.
- **Storage-risk visibility added after P0:** the control desk now presents a red action-required storage panel and includes off-sync storage in operating-rule readiness. The API exposes only the provider and safe/unsafe state, never the private path, and performs no relocation.
- **Private-data relocation is now safely rehearsable:** `tools/relocate-data.ps1` defaults to a zero-write dry run, refuses project-internal/cloud-synchronised destinations, requires an explicit stopped-server confirmation plus an exact typed phrase for copying, never overwrites or deletes, and verifies every copied file plus the unchanged source by SHA-256. Synthetic regression coverage is part of the full suite. The live folder was inspected only in dry-run mode: five private files remain in OneDrive pending founder approval of the destination, backup and access controls.
- **E3 takeover hardening plus Google and Facebook sign-in complete in detached source:** verified Google email may deduplicate automatically only into an already verified social-only account. Facebook's email remains untrusted: a new App-bound subject must consume Tideway's own single-use mailbox-verification link before account creation or social-only linking. Pre-authenticated callbacks cannot attach to password, inactive or unverified accounts. Password accounts use persistent-lockout-aware current-password step-up in `/settings`; social-only accounts can now prove an exact existing provider subject and receive one short-lived, user/session-bound approval to connect another method. Removal requires password step-up or proof through the different provider that will remain, refuses the final method and atomically revokes every session. Local PostgreSQL 16.14 exercises each boundary. Both public providers remain disabled until managed PostgreSQL/SMTP, HTTPS origin, app review and verified credentials pass.
- **Account-first booking journey complete behind the attachment gate:** every public Book/Request cleaning action opens `/signup?intent=book`. Configured Google and Facebook starts carry only that fixed action in signed, expiring provider state; their callbacks automatically create or safely reuse the verified Tideway account and preserve the action through Landlord onboarding. Email/password verification and resend links carry the same fixed action across a new tab or device and return a verified customer to booking sign-in. After Landlord confirmation, a first-time account immediately receives the open property form; a returning Landlord with saved properties goes directly to the room-scan request form, and a newly saved property continues into that request automatically. Saving the request then opens its required private scan, loads its current state and focuses the room selector. Successful scan submission leaves the editor for a dedicated private thank-you state with the request reference, photo/task counts, truthful matching boundary and tracking/new-request actions; an uncertain follow-on dispatch response never invents an invitation result. The browser remembers the allowlisted action for 30 minutes, Landlord is preselected but still confirmed by the user, and an existing Cleaner-only account is never silently changed. Arbitrary or duplicated intent/start values are rejected to prevent open redirects. Provider controls remain hidden locally until managed infrastructure, the final HTTPS domain and approved credentials are configured.
- **A selected Cleaner now survives the shortest booking journey:** each real directory card carries only its validated public Cleaner UUID into 30-minute same-origin browser storage before account creation. Google, Facebook, email verification across tabs, onboarding and the Landlord dashboard preserve that selection without placing prices, contact details or a redirect in browser state. After the required room scan, the customer sees one explicit preselected invitation choice; submission calls the existing protected invitation route, where the server independently rechecks ownership, complete scope, service fit, availability, overlap, server-owned pricing and positive margin. A stale/ineligible/unpriceable selection leaves the safely submitted request open for ordinary matching, clears the browser choice and truthfully confirms that no booking or payment exists. Focused intent, directory, dashboard, authentication and HTTP regressions pass locally; marketplace attachment and genuine profiles remain disabled pending managed staging.
- **Authentication activation is now one secret-safe preflight:** `pnpm run preflight:authentication` validates the complete production deployment boundary, attached marketplace prerequisites, email fallback and the explicitly selected Google/Facebook credential sets before staging. It emits the exact canonical Homle callback URLs and remaining live evidence without printing database, Administrator, storage or provider secrets. Missing or duplicated provider selection, partial credentials, detached marketplace mode and unsafe deployment configuration fail closed. A pass remains configuration evidence only; managed probes, the external verifier and two non-customer account tests are still required before buttons become public.
- **Every pilot proposal now shows its profit result where the price is entered:** the matched-Cleaner draft form calculates customer total, Cleaner pay, non-Cleaner costs, contribution and margin live from the founder-confirmed assumptions. It blocks saving when assumptions are unconfirmed, the reviewed/minimum duration is undercut or the recorded margin floor is missed, and offers one **Use £X/hour minimum** action calculated from the exact hours, pay and additional costs. The existing server calculation remains authoritative and rechecks the same frozen cost model before draft creation, offer handoff and booking.
- **Public conversion path simplified without deleting the pilot fallback:** `/` now serves a lightweight, mobile-first homepage with direct Book, Find a Cleaner, Work as a Cleaner and Sign in actions plus a four-step book/scan/review/follow explanation. It contains neither long intake form nor form-processing bundle. The operationally safe concierge forms remain isolated on `/request` and `/join`; no intake record, account, provider or launch setting was changed.
- **Public booking entry now remains usable before account activation:** the no-script homepage defaults every customer and Cleaner-search action to the working guided `/request` flow and hides unusable sign-in. A privacy-minimal `/api/health` check upgrades those actions to account-first booking, directory search and sign-in only when the marketplace, its dependencies and authentication attachment are all genuinely ready. Network failure or malformed health state stays on the honest fallback. Disabled account, room-scan, tracker and directory recovery links now return to that same working request path, and the pilot page's booking actions jump directly to its form. No service is enabled and no request is submitted automatically.
- **Cleaner search reduced to one simple decision pair:** `/cleaners` now shows only outward postcode and service before its primary search action. Rating, price, availability and recorded-verification controls remain under one optional disclosure. Every real result exposes a visible **Start a cleaning request** action that enters `/signup?intent=book`; the legacy `/request` form is no longer the directory handoff, and the card still states that viewing a Cleaner does not reserve or confirm them.
- **The mobile web app now has a complete installed identity:** the manifest has a stable Homle ID/scope, description, language, standard and maskable phone icons plus direct request/Cleaner shortcuts. iPhone home-screen metadata and a dedicated Apple touch icon are present on both homepage entry files. Authenticated pages, room media, messages, access details and location remain network-only because no service worker caches private data. The web limitation on background journey tracking remains documented; no native-app capability is claimed.
- **Cleaner discovery now has no dead end:** unavailable accounts, zero matching profiles and connection failures each keep the honest state explanation while presenting one working guided-request action. Retry remains available where useful, but a customer can always continue to `/request` without waiting for the detached account marketplace.
- **Ordinary household cleaning is now complete in the working pilot path:** the customer request offers **Regular home clean**, the Cleaner application offers **Regular home cleaning**, private recovery drafts preserve that choice and the server maps the two sides to the same approved service. Unsupported service labels are rejected before storage. The full first-booking smoke journey now runs this domestic path through matching, reviewed scope, availability and profitable proposal controls instead of relying only on rental-turnover work.
- **Uncategorised cleaning can no longer match every Cleaner:** the public pilot now requires one of the six supported service choices, backed by a single server policy shared by intake, eligibility, matching, proposal, quote, opportunity and booking checks. A pre-existing `Not sure yet` record fails closed with a specific operator explanation and cannot produce matches or advance an offer. An isolated HTTP regression rewrites only synthetic test data to prove the legacy boundary, and the production container allowlist includes the new policy module.
- **The public request removes one avoidable customer decision:** choosing a home, short-let, office, communal or other commercial property now suggests the corresponding supported cleaning service and explains that it remains editable. The suggestion continues to follow property changes only while it is automatic; a restored draft or any manual service choice is preserved. The room scan still confirms the exact tasks, and the server remains authoritative about supported service names.
- **Room photos no longer repeat the spoken checklist:** both the working private pilot scan and the detached authenticated Landlord scan require a supported room label and an actionable room-labelled Cleaner checklist, but no longer force a second manual description for every visual. A photo note remains available for genuinely useful visual context. When it is left blank, the server stores neutral room-checklist context rather than inventing image contents. Media consent, shown-room task coverage, final scope confirmation, private storage and Cleaner access controls are unchanged.
- **The working request no longer asks two premature questions:** the public pilot has removed the visible customer-role classification and general access-arrangement fields because neither is needed to check service, scope, coverage or timing. The server owns neutral values for old clients and direct API submissions. Exact entry instructions still remain a separate protected post-acceptance step, while site size, known hazards, requested service/date, contact details and consent remain required.
- **One reliable contact route now completes the public request:** contact name and email remain required for the private scan and request updates, while phone and organisation are grouped behind one optional disclosure. A provided phone is still validated and remains available to operations, but an otherwise valid customer is no longer blocked for withholding a second contact method. Cleaner applications still require a phone because their separate screening and job-day contact process depends on it.
- **The primary timing step now contains only real booking decisions:** known hazards, preferred date and arrival window remain visible. Recurrence and free-text job notes moved into one optional disclosure, retaining the one-off default and all server-side validation. This removes optional form weight without guessing a date, hiding hazards or weakening the post-booking access-secret boundary.
- **The required room scan now replaces a duplicate size question:** the working request's primary job step asks only postcode, property type and supported service. An approximate size/room hint remains under one optional disclosure for large or unusual sites. If it is omitted, the server records that the room scan must supply scope; once the reviewed scan exists, quotes, Cleaner opportunities, customer tracking and both booking packs use its confirmed room labels instead of an invented size. The full profitable-booking journey now passes without a typed size while still requiring reviewed visuals, checklist confirmation and the scan-derived time floor before matching or booking.
- **Mobile room capture now happens from one panel:** after choosing the current room, the working private scan unlocks the rear-camera action, existing media and speech together. Photos, videos and spoken notes all inherit the selected room, and speech cannot start without that room context. The next panel is now a focused transcript review instead of making the Landlord scroll away from the camera controls to begin speaking. Unsupported browsers retain the typed fallback and all checklist, consent and server-side scope gates remain unchanged.
- **Short room video now has its own direct mobile capture:** beside the photo action, the scan offers **Record a room video** with a rear-camera capture hint, inherits the selected room and feeds the same duration, count, per-file, total-upload, preview, consent and server checks as gallery video. It requests one capture rather than a multi-file library selection, while honestly noting that some mobile browsers may still present their own camera/gallery chooser. Existing-media selection remains separate.
- **The production app now owns the canonical-domain redirect:** safe `GET` and `HEAD` traffic arriving at `www.<APP_ORIGIN host>` receives a path-and-query-preserving 308 to the exact configured HTTPS origin. Canonical traffic remains untouched, and non-canonical mutations are never replayed through a redirect; they continue into the existing origin/CSRF denial. Focused production-process coverage proves all three boundaries. The change is prepared locally and is not active on Hostinger until the next approved redeploy.
- **Cleaner first contact shortened to protect supply conversion:** `/join` no longer asks applicants to build or preview a professional profile before applying. The first application is limited to identity/contact, explicit matchable areas/services, experience and one future window; optional notes are collapsed. Biography, languages and equipment planning are a separate private-tracker step, and the server ignores eager profile fields on initial intake so screening cannot accidentally rely on a bypassed browser. The working profile, screening and approval gates remain unchanged.
- **Cleaner profile completion reduced to one guided decision:** `/cleaner/profile` now opens the first incomplete section, presents introduction, services/pricing, work boundaries and review as four short steps, shows exact per-section completion and keeps one next action prominent. Progress can be saved without completing the whole profile; the existing ten-requirement publication gate and server-side validation remain unchanged.
- **Account entry now leads with the fastest available method:** when social sign-in and email are both attached, `/login` and `/signup` show Google/Facebook first and reveal the email fallback with one accessible action. A fixed Book-a-clean intent reduces onboarding to one explicit preselected Landlord/Property Manager confirmation and hides the irrelevant Cleaner option; it does not silently change an existing account role. Provider capability gates, email verification, signed intent, session rotation and server authorization remain unchanged.
- **Private booking updates are now visible without dashboard hunting:** both Landlord and Cleaner workspaces link to one mobile-first `/notifications` inbox. It turns lifecycle events into concise next-action cards, validates each booking route, supports unread totals, cursor pagination, loading/empty/offline/retry states and a race-safe **Mark all read** action that reloads authoritative state. The existing account-bound service and PostgreSQL functions still own access; the browser never renders raw payloads or private property/message/location details. Email delivery remains disabled until a real SMTP account passes staging.
- **Unread work is visible from both role dashboards:** the mobile navigation now shows a bounded unread count beside **Updates**, refreshed on the initial private account view and after returning to the page without constant polling. Invalid, signed-out or unavailable responses display no badge and never block the dashboard. The badge reads only the account-scoped notification count and cannot expose payload, address, message or location data.
- **Private booking-case notifications no longer fail silently:** migration 033 queues `dispute-opened`, `dispute-reviewing` and `dispute-resolved` email work, but the detached worker previously rejected all three as unknown and the service discarded their safe status fields. The worker and mobile inbox now provide privacy-minimal next-action copy for every case stage, expose only case ID/status/outcome identifiers, and continue withholding descriptions, resolution notes, property/contact/location data and payment details. A database-to-worker parity regression now fails if any queued email event lacks executable worker copy.
- **Every lifecycle update now has one correct private next action:** instead of sending participants to the homepage, new invitations open the Cleaner dashboard containing Accept/Decline, decline/expiry updates open the relevant role workspace, and active booking/journey/progress/message/review/case events open the exact `/bookings/{bookingId}` screen. Email and inbox routing start from the validated opaque booking UUID, add no query, fragment, tracking token or private property/contact content, and leave authorization to the destination route. Focused regressions cover both roles, reopened matching, malformed identifiers and homepage fallback.
- **The agreed total is visible before payment begins:** the owner-authorized payment read now returns a safe `not-started` projection with the exact frozen GBP booking total even before a payment row exists. `/booking-payment` no longer asks a Landlord to “prepare” an unknown amount; it shows the total first, presents one **Enter secure payment details** action, labels final authorization with the exact amount and collapses secondary security explanation. No authorization is created automatically, and the browser still cannot set price or load Stripe before the deliberate action.
- **Account speech/checklist confirmation is now voice-first and change-sensitive:** the Landlord sees one large speech action instead of two competing long text areas. Stopping speech automatically updates a readable room-grouped bullet review; the raw walkthrough and manual task editor stay collapsed, while unsupported browsers open the typed fallback automatically. Only concise room tasks are sent. Editing the typed/spoken walkthrough, manually changing tasks, generating a new summary or copying a reusable checklist clears any prior review confirmation and tells the Landlord to summarise again or reconcile every task. Final scan submission still requires the separate server-enforced checklist/photo confirmation.
- **First property setup no longer asks for two unnecessary decisions:** the required path is type, address line 1, town and postcode. Custom property labels and address line 2 moved into the optional disclosure; when no label is supplied, the server creates a privacy-safe type-and-locality name rather than reusing the protected street address. A sole saved property is selected automatically when the Landlord starts a request. Property ownership, exact-address encryption and accepted-booking-only Cleaner access remain enforced.
- **Landlords can now remove stale pre-booking demand safely:** draft and searching requests expose one accessible **Withdraw request** action with a required reason. Migration 045 locks the owner-bound request, refuses every request with a live invitation/booking, revokes queued automatic dispatch and records status history plus an audit event. It cannot cancel a confirmed booking or mutate payment; later lifecycle states expose no withdrawal control. Focused service, repository, HTTP, database-asset and mobile UI tests pass locally.
- **The obvious cleaning category is suggested, not demanded:** homes default to regular domestic, office/retail/clinic properties to workplace cleaning, and communal properties to communal areas. “Other” remains blank, and once a Landlord changes the selection the browser never overwrites it. The category remains visible and editable because rental turnover, end-of-tenancy and deep cleans cannot be inferred safely from property type.
- **Cleaner checklist quality is now enforced before matching:** both the working private room scan and the authenticated Landlord request flow accept concise action lines and explicit leave-alone boundaries, but block placeholders, meaningless text, noun-only notes and catch-all scope such as “clean everything”. The live readiness panel names the affected count and gives a concrete correction example; the server repeats the same rule so a bypassed browser cannot freeze an unusable Cleaner handoff.
- **Cleaner supply can now stay current during review:** an applicant may add exact future availability through the high-entropy private tracker immediately after applying instead of waiting for screening to finish. Each non-overlapping update is retry-safe, visible only as pending and excluded from matching. The control desk offers no confirm choice before all seven screening checks and the separate approval decision are complete, and the server repeats that gate; paused or closed applications cannot submit. A passed first window is no longer prefilled into the form.
- **The first-job quote floor now matches enforcement exactly:** the private control-desk calculator, no-save launch rehearsal and proposal server share one penny-rounded economics model. Cleaner pay, labour on-costs, payment fees, travel, supplies, risk and additional costs are rounded as they will be enforced, then the displayed minimum rate advances by pennies until it genuinely clears the founder-approved contribution-margin floor. A regression proves the former algebraic £28.90/hour result fails a 9.5% floor after component rounding while the exact £28.91/hour result passes; no quote, payment or launch setting was created.
- **One-action Cleaner payout onboarding complete behind the payment gate:** `/cleaner/payouts` gives an authenticated Cleaner one relevant action, creates or safely reuses a server-owned Stripe test connected-account reference, redirects only to Stripe-hosted onboarding and refreshes verified readiness after the HTTPS return. The browser cannot submit a payout destination or readiness flag; bank details never enter Tideway; both private payout tables are function-only and important changes are audited. Stripe and the marketplace remain disabled, live keys remain rejected and no provider account, permission or payment was touched. Founder approval of the legal/payment model plus a real Stripe test platform and two-account HTTPS cycle remain required.
- **Business readiness can no longer be confused with an activated marketplace:** the private control desk now shows a separate six-check score derived from the running environment. It independently requires safe local private-data placement, the verified PostgreSQL/email/private-media attachment, attached email account services, an attached Google or Facebook provider, the test-only payment service and production mode with every local demo disabled. It returns booleans and the next safe action only—never secrets, service addresses or private paths—and does not switch on, publish or purchase anything. The final two-account booking, payment and mobile rehearsal remains visibly separate.
- **Real Cleaner profile interfaces complete behind the attachment gate:** `/cleaners` is a real API-backed, safely rendered search interface with honest unavailable/empty states and no fixture people. `/cleaner/profile` is an owner-only mobile editor that preserves unchanged private area coordinates, validates exact prices and every profile field, requires session/CSRF protection and blocks incomplete publication. Real profiles remain absent until genuine account onboarding and PostgreSQL staging pass.
- **Real Landlord account workspace complete behind the attachment gate:** `/landlord/dashboard` lists and creates only the signed-in owner's private properties, turns speech into editable room-by-room bullets and saves exact future cleaning-request drafts. The browser always sends `submit: false`, so no matching, invitation, booking or payment can occur. Successful Google, Facebook and email sessions now hand established roles to the correct workspace; media capture remains on the working `/request` pilot until private account storage passes staging.
- **Real participant active-job interface complete behind the attachment gate:** `/bookings/:bookingId`, `/tracking` and `/cleaning-progress` now open one authenticated mobile screen backed by the participant-only journey, progress, property and durable event APIs. Cleaners deliberately start or resume foreground location, record arrival, start/pause/resume work, update tasks/issues, propose unexpected work and finish only after every task resolves; Landlords receive live status and control unexpected-task approval at the frozen price. Browser watches stop at arrival/terminal state/page exit, no external map receives coordinates and unavailable infrastructure fails closed. Approved map/ETA selection and the final HTTPS two-device trial remain open.
- **Unexpected work can no longer become hidden unpaid overtime:** a Cleaner must explicitly confirm that a proposed task fits the remaining booked time and already agreed pay, the server records that confirmation, and the database checks the estimate against the scheduled end both when proposed and when approved. The Landlord decision stays locked until the Cleaner confirmation exists. Anything needing more time or pay is directed to the existing issue path for separate scope review; legacy pending tasks can be deliberately reconciled without backfilling consent. Migration 039, least-privilege grants, attachment/deployment probes and service/HTTP/mobile regressions cover the boundary.
- **Payment reconciliation is now exactly-once and direction-safe:** only one refund may be in flight, a Cleaner transfer cannot start during a refund, and a refund cannot start after a pending or completed transfer until a verified reversal is recorded. Webhooks must match the exact command kind, amount, currency and permitted prior state; a second event for one command becomes a semantic duplicate instead of adding the same refund twice, while a real transfer reversal remains allowed. A provider response arriving after its webhook cannot reopen a reconciled, failed or reversed command. Migration 040, the deployment/attachment index probes and a disposable-database payment-ordering scenario are prepared without enabling or contacting Stripe.
- **Confirmed bookings no longer offer payment before the hold window:** migration 042 makes the participant summary calculate the exact five-day opening time, withholds the payment action until the server can accept it and shows when an exact authorization is ready for the scheduled start. The Landlord dashboard explains “no action needed yet” instead of leading to a predictable rejection. Cleaner summaries receive none of the readiness fields, and provider references, client secrets and retry hashes remain private.
- **Payment follow-up now begins early without notification spam:** migration 043 queues a calm “payment is now open” update between five days and 24 hours before a confirmed clean, followed by one urgent 24-hour reminder only if the exact authorization remains incomplete. Both open the private booking payment step, use independent once-per-schedule keys, remain bounded under the function-only worker and cannot change a booking or money.
- **A paid confirmed clean no longer goes quiet before arrival:** migration 044 queues one private reminder for each participant inside 24 hours only after the exact authorization is valid for the scheduled start. Inside two hours it queues a separate Cleaner-only prompt to open the active job when ready to set off. Every key includes the exact schedule, rescheduling creates a fresh reminder sequence, and the bounded restricted worker changes no booking, payment or location. Email and inbox copy expose no address, access detail, price, contact, photo or coordinates.
- **Private booking chat connected to the active-job screen:** Cleaner and Landlord messages use the existing participant-only service, server-enforced contact-detail blocking, CSRF-protected idempotent sends, stable earlier-history cursors and the durable no-poll event stream. The browser preserves the retry key after a lost response, safely deduplicates live delivery and renders no message as HTML. Real two-account PostgreSQL/HTTPS evidence is still required before activation.
- **Private before/after job evidence connected to the active-job screen:** after arrival the Cleaner can take one rear-camera image or choose an existing supported photo, label it before/after/issue and attach it to a checklist task. The browser size/type-checks and SHA-256 hashes locally, uses only the server's exact signed headers, omits Tideway credentials/referrers, blocks redirects, retains safe retry state and waits for server sanitation before showing metadata. Participants request five-minute private views; the page clears each signed URL on close. The active-job CSP permits only validated exact storage/bucket origins, while camera stays denied everywhere else. Real bucket/CORS/lifecycle/content-threat and HTTPS device evidence remains required.
- **Verified ratings connected to the completed-booking experience:** the Landlord now confirms the Cleaner-finished visit after checking tasks, notes, issues and private photos, then receives one overall/category/written review form. The Cleaner sees nothing until approval and can add one final professional response. Client states never bypass the completed-only, participant-only, one-per-booking, moderation and approved-only aggregate rules already enforced by PostgreSQL. Real two-account PostgreSQL/HTTPS evidence and Administrator moderation operations remain required before activation.
- **Automatic-dispatch prerequisite hardened:** the final invitation transaction now independently rechecks the active Cleaner account, public profile, property preference, exact current service pay, full availability, postcode/radius coverage and every overlapping pending/confirmed job. Competing invitations for one Cleaner are serialized, and neither restricted role can execute the superseded functions. The local PostgreSQL acceptance race now passes; managed multi-worker dispatch lease/expiry evidence remains required before scheduling automatic matching.
- **Consent-bound automatic dispatch complete in detached source:** a Landlord must explicitly authorize each submitted future request and choose a total one-to-five attempt limit. A dedicated leased worker uses the existing profitable matching policy, excludes every previously attempted Cleaner, falls through when a candidate becomes stale and creates at most one hardened invitation before expiry/decline can requeue it. The app cannot directly update or delete request lifecycle fields and the worker has only narrow functions. PostgreSQL concurrency, approved pricing, genuine Cleaner supply, delivery/expiry and monitoring evidence remain required before scheduling it.
- **Authenticated private room-scan submission complete in detached source:** every account request begins as a private draft. The Landlord adds one-to-ten room-bound still photos through checksum-bound quarantine uploads; Tideway re-encodes them as metadata-free private JPEGs before recording them. At least one completed photo, reviewed speech/checklist scope and an explicit Cleaner-preview choice are required before function-only submission freezes a combined scan/scope fingerprint and opens matching. Optional automatic dispatch remains a second bounded consent. An invited Cleaner sees the scan only with pre-acceptance consent and the assigned Cleaner retains it after confirmation; exact address/access details and storage keys remain hidden. Migration 030, migration 031, both role grants and the disposable PostgreSQL 16.14 harness now pass locally; managed database/bucket/CORS/HTTPS two-device evidence remains required and the marketplace flag stays off.
- **Real participant booking dashboards complete behind the attachment gate:** `/cleaner/dashboard` lists assigned invitations and active/upcoming/history records, lets the Cleaner accept or decline with session/CSRF protection, and links confirmed work to the active-job screen. `/landlord/dashboard` now lists the Landlord's active/upcoming/history records and role-safe payment/job actions. Migration 026 owns participant isolation and returns only Cleaner pay to a Cleaner or customer total to a Landlord; precise addresses, access instructions, contact data, platform margin and the other side's price are excluded.
- **Cleaner acceptance is now one explicit tap:** each invitation shows the Cleaner pay once, labels it clearly, and puts the exact amount in the **Accept £X job** button. Inline copy explains that tapping confirms availability for that exact time and pay; the server still owns expiry, eligibility, scope, current pay, availability, overlap and profitable-term rechecks. The native browser confirmation was removed as redundant friction, while decline retains its reason/confirmation dialog because it reopens matching.
- **Active cleaning tasks are now one tap by default:** eligible unresolved items show one large **Mark task complete** action with a visible saving state and task-specific accessible name. Status changes, notes, skips, issues and corrections remain in a compact detailed control. Pending or declined unexpected work stays locked, while Landlord-approved unexpected work becomes actionable. The participant-only server update, durable audit event and unresolved-finish gate are unchanged.
- **Audited booking-case workflow and Administrator screen complete behind the attachment gate:** either exact booking participant can open one private bounded case from the active-job screen; lost-response retries return the same record and opening pauses the booking without changing money. The app role has no direct dispute-table access. `/admin/cases` verifies an Administrator account before loading its bounded filtered queue, displays only shortened booking and minimum case facts, supports review/resolution with CSRF, truthful no-payment confirmation and offline/retry states, and never renders contact or access details. Category-specific `tideway-case-response-v1` guidance now routes the minimum useful evidence and states the safety, privacy and payment boundaries; the server rejects a resolution without current-version evidence/minimisation/no-external-action assurances. The disposable PostgreSQL 16.14 suite proves unrelated-user denial, one-active-case enforcement, function-only writes and exact Administrator resolution, then removes every fixture. Founder-approved response targets, retention, refund/re-clean/compensation authority, escalation ownership, approved Administrator provisioning and real HTTPS visual/two-account staging remain open.
- **First-Administrator bootstrap prepared but not executed against a real account:** migration 034 exposes one migration-owner-only function for an existing active, verified email/password, Google or Facebook account. The web and worker roles cannot execute it; the first grant is globally serialized, audited and retry-safe, all target sessions are revoked, and any second bootstrap is rejected. The disposable PostgreSQL 16.14 run proves restricted-role denial, exact retry, changed-retry rejection, two-session revocation and zero remaining fixtures. Real execution requires explicit founder approval for that exact permission change after managed deployment and final-domain authentication.
- **Account privacy-request intake complete behind the attachment gate:** authenticated customers can request a data export or account deletion and track its honest review status in `/settings`. The app role has no direct privacy-request table access; actor-bound functions serialize one active request per type, make lost-response retries idempotent and write minimal audit evidence. This deliberately does not auto-delete an account, cancel a booking, change money or invent a completed export. Administrator fulfilment, secure delivery, retention decisions and response ownership remain open.
- **PostgreSQL source activation gate added:** all 45 ordered migrations plus the runtime/worker least-privilege grant scripts are SHA-256 locked and checked for missing, unexpected, reordered, modified or transaction-boundary-damaged assets. Migration 031 preserves deployed history while correcting the Cleaner service-area lookup defect found by real execution; migration 032 adds exact-subject social step-up and lockout-safe provider removal; migration 033 adds function-only booking cases; migration 034 adds owner-only first-Administrator provisioning; migration 035 adds function-only account privacy-request intake; migration 036 adds actor-bound Cleaner payout onboarding; migration 037 exposes the exact owner-authorized booking total before payment creation; migration 038 adds Meta's signed deletion callback/status boundary; migration 039 protects the frozen time/pay boundary for unexpected active-job tasks; migration 040 enforces exactly-once and direction-safe refunds/transfers; migration 041 queues a once-per-schedule payment-readiness warning without mutating money or booking state; migration 042 keeps dashboard payment actions inside the exact authorization window; migration 043 adds calm-open and urgent two-stage payment follow-up; migration 044 adds authorization-gated participant and Cleaner journey reminders; migration 045 adds audited owner-only withdrawal for draft/searching requests and refuses live booking cancellation.
- **All 38 migrations and the expanded PostgreSQL verifier now pass locally:** on 17 July 2026, the guarded bootstrap applied the exact locked set from an empty loopback-only PostgreSQL 16.14 database under a separate non-superuser, non-BYPASSRLS migration owner, then applied both restricted-role grant files. The read-only audit passed its exact 40 RLS-table, 42 app-function and 13 worker-function inventories, ownership, critical constraints/indexes, trusted function paths, protected-table revocations and direct denial of the private Facebook deletion-confirmation table. Credentials remained out of process arguments.
- **Current PostgreSQL behavioural integration passes locally:** a clone of that 38-migration schema used distinct owner/app roles, reserved `invalid.example` fixtures and two simultaneous `psql` transactions. It proved known-subject Facebook deletion queueing, opaque retry stability, honest unknown-subject completion, request-ID collision denial, status isolation and private-table denial; ownership RLS; unrelated-user denial; function-only privacy intake; pre/post-acceptance access-instruction privacy; revoked direct workflow writes; current-payment journey gating; disputes; exactly-once history; and one protected schedule conflict under a real acceptance race. The restricted worker then passed 13 functions and all five maintenance jobs. Reserved users, bookings, deletion callbacks and privacy requests were all confirmed at zero before both disposable databases and the temporary owner role were removed. Managed staging, provider-backed services and multi-instance evidence remain open.
- **E1 fail-closed attachment now owns database, client identity, SMTP and private media boundaries:** the default-off attachment constructs a shared limiter, trusted proxy resolver, strict-TLS mailer and private S3-compatible storage internally. Storage permits only server-generated quarantine/final job-photo keys, signs exact checksum/encryption headers, bounds streamed source bytes/pixels/pages, applies orientation, flattens alpha, strips metadata through JPEG re-encode, records an output checksum and issues five-minute no-store reads. PostgreSQL, SMTP and bucket access are verified before routes attach; every resource closes on failure. Exact `pg`, Nodemailer, matched AWS SDK v3 and Sharp packages are full-lockfile gated. Real provider/bucket/CORS/threat/proxy evidence and deployment monitoring remain outstanding, so the flag stays false.
- **Domain/HTTPS and social-provider cutover gate prepared:** `tools/domain-readiness.mjs` performs a read-only external check of the exact canonical origin, public-only DNS, trusted TLS lifetime, permanent HTTP redirect, homepage content type, CSP/HSTS/browser policies, Tideway health, API cache prevention and truthful authentication discovery. It now also requires the production `localDemosEnabled: false` signal, an anonymous non-cacheable JSON 401 from `/admin`, and non-cacheable JSON 404s from every read-only tracking-lab surface without redirects or cookies; it never sends an Administrator key or creates a lab session. It defaults to proving Google/Facebook are closed; after approved staging an explicit `TIDEWAY_EXPECT_SOCIAL_PROVIDERS` list requires the matching capability and validates the provider-specific start redirect, canonical callback, secure host-only flow cookie and no-store response without following the redirect or exposing flow material. Apple cannot be selected because its flow is not implemented. Deterministic tests cover closed and enabled Google/Facebook states plus private DNS, short certificate, redirect, header, health, exposed local tools, anonymous control-desk redirects/cookies and provider failures. The founder still must purchase/provide the hostname and approved hosting; no DNS or public deployment was changed.
- **Production-process preflight prepared:** the documented first public-site deployment can now run with marketplace and payments genuinely disabled rather than requiring fake database credentials. `pnpm run preflight:production` and the server's own pre-listen gate require the exact public HTTPS origin, explicit binding/port, `LAN_PORT=0`, protected 32+ character Administrator secret, trusted immediate-proxy CIDRs and an absolute off-source/off-cloud private-data directory. Marketplace mode adds the managed SMTP, private storage and monitoring-adapter requirements, while payments are rejected whenever the marketplace is detached. A real production-mode child process passed `/api/health` with marketplace false and shut down without contacting infrastructure. Hosting selection, real proxy ranges, encrypted persistent storage, domain/DNS and external verification remain founder/staging work; nothing was published.
- **Local real-location lab removed from the production surface:** the homepage and labelled sample preview no longer link to `/tracking-test`. Development still serves the loopback-only two-role lab at its direct address, preserving current-point/arrival/progress testing. Production does not construct the lab store or timer and returns generic 404 responses for its friendly/direct assets and every API before origin, token or rate-limit processing. The production child-process test proves the health flag is false and the page, direct HTML/script and session creation are unavailable. This does not replace the authenticated booking journey; real two-phone HTTPS tracking remains a staging gate.
- **Host-neutral production container prepared but not built:** the two-stage Docker artifact verifies the locked production dependency graph, copies an explicit runtime allowlist, excludes secrets, private records, tests, launch notes and browser tracking-lab assets, runs as the unprivileged Node user, defaults marketplace/payments off, uses off-source private storage, handles graceful stop and checks the Tideway health contract. Static regression coverage guards every boundary. Docker is not installed on this development computer, so the selected Linux host must still prove the image build, native Sharp/runtime imports, resolved image digest, vulnerability scan, persistent-volume ownership, read-only filesystem and proxy behavior before any deployment; `pnpm audit` does not cover base-image packages.
- **Pre-booking access-secret collection closed:** the working `/request` pilot now asks only for a general access approach and explains that exact instructions are collected privately after acceptance. One shared browser/server rule blocks door, gate, alarm and key-safe codes, passwords and exact hidden-key locations across the access, site-size and job-detail fields before any record is written; safe approaches such as reception, concierge or office key collection still pass. The 30-minute tab recovery draft removes a secret from any of those fields instead of persisting or restoring it. Unit, draft and isolated HTTP regressions prove helpful rejection, bypass resistance and zero lead write. Final exact access still uses the existing protected confirmed-booking pack.
- **E7 concrete shared limiter complete in detached source:** fifteen exact authentication/directory/review/Facebook-deletion scopes now use an atomic PostgreSQL fixed-window function. The Node adapter purpose-HMACs the trusted client key separately per scope, so raw IP/key material never reaches storage and buckets cannot be correlated across scopes. Web/worker roles have no direct counter access; the worker alone can purge two-hour-inactive buckets in bounded `SKIP LOCKED` batches. Bounded denials return `Retry-After`; malformed decisions/outages fail closed with private monitoring. The restricted local purge executes successfully; managed continuous scheduling and two-instance shared-throttle evidence remain outstanding.
- **Default-off 24/7 worker process complete in source and locally database-proven through migration 038:** the separate supervisor schedules invitation/location/payment-readiness/confirmed-visit/session/rate-limit/social-identity maintenance without overlap, reports failures only to the private monitor, exposes privacy-safe health and closes cleanly. Its real entrypoint remains alive after startup and handles an interrupt with a clean resource shutdown. Email, media cleanup and automatic dispatch are independent opt-in capabilities. The guarded predecessor run authenticated as `tideway_worker`, proved function-only access to 13 functions and ran five maintenance jobs once; migrations 041 and 044 extend the source boundary to 15 functions and seven jobs and require the guarded managed-staging rerun. Managed continuous scheduling, alerts, provider-backed optional jobs and two-instance evidence remain open.

---

## 0. How to use this document

- Work the queue **top to bottom**. Sections are ordered by dependency: P0 (fix now) → P1 (make it a real app that takes payments) → P2 (make it truly automated).
- Every task has: **Owner** (agent or founder), **Files/anchors**, **Steps**, **Acceptance criteria**, **Validate**.
- Prefer **searchable anchors** (function/string names) over line numbers — line numbers drift; the anchors are exact.
- Do **not** start a task marked **Owner: Founder**. Instead, if it blocks you, leave the code ready behind a capability flag (the repo already uses this "build detached until infra exists" pattern) and note in your commit what founder input is required.
- After any change, run the validation in §7. Do not mark a task done if `npm test` or `npm run check` fails.

---

## 1. Where things stand (read once)

This repo contains **two runtimes**:

1. **The live pilot** — `server.mjs` (~5,094 lines) + `public/`. Its active NDJSON path remains dependency-free while the marketplace flag is false. This is what runs today. It boots clean and `GET /api/health` returns healthy. It does **not** take payments and has **no** production login. Post-scan steps (quote/match/book) are **manual** via `/admin`.
2. **The detached marketplace** — `src/marketplace/*` + `db/migrations/001–045` (PostgreSQL, RLS, auth, bookings, matching, consent-bound dispatch, tracking, reviews, disputes, privacy requests, private media, shared limiting, payments, Cleaner payout onboarding, maintenance and first-Administrator bootstrap). Exact database/mail/S3/image dependencies plus the limiter, SMTP delivery, object storage and client resolver are integrated behind the default-off attachment. The complete source and behavioural harness pass locally; migrations 039–045 still require the guarded real PostgreSQL staging run. **It still cannot be enabled safely:** managed PostgreSQL, SMTP and a real private bucket have not passed staging; CORS/lifecycle/public-access/threat controls and final proxy topology are unverified; approved monitoring remains absent (F2, F8).

The intended product is an Uber-style cleaning marketplace: *request → room scan → checklist → match one cleaner → both accept → confirm → track → complete.*

**Latest UX closure (16 July 2026):** authenticated Cleaners can now add exact future availability through one focused mobile-first screen instead of a vague profile label. The dashboard makes this the next action when no future window exists. The server owns role/CSRF checks, timezone-aware bounds, serialized overlap prevention, booking-safe withdrawal and automatic matchability status. No migration was required because the existing RLS-protected `cleaner_availability` model is reused. Managed staging remains required before this detached marketplace path can serve real accounts.

Cleaner publication also no longer requires an email-based applicant to paste an externally hosted image URL. Verified social-login photos remain server-owned when present; otherwise the public initials fallback is used. Completion now measures nine real profile decisions, reducing first-supply friction without inventing identity or verification evidence.

The Cleaner dashboard now reads authoritative profile readiness alongside bookings and availability. Its one next action follows the actual supply gate: answer urgent work, complete/publish the profile, add exact availability, then configure payouts. This prevents a new Cleaner being sent into schedule or payment setup while still invisible to matching.

The Landlord dashboard now evaluates the complete booking/request lifecycle before choosing its single next action. Live work takes priority, then a confirmed booking requiring authorization, then confirmed work and the active matching request before any new draft. An earlier waiting invitation can no longer hide a later payable booking or push the Landlord toward a duplicate request.

**The app is ~40% of the business.** The rest is cleaner supply, trust/insurance, and moving money — those are founder tasks and set the real timeline (see §8).

---

## 2. Non-negotiable safety rules (already in this project — keep enforcing)

- **No spending, no Polsia credits, no publishing, no outreach, no contracts, no hiring/rejection, no charging, no payments** without explicit founder approval.
- **Never publish or invent trust claims** (ratings, "DBS-checked", "insured", "10,000+ cleans", coverage areas). Keep all public copy honest and pilot-stage.
- **Never contact real applicants/customers** or send any message automatically.
- **Never commit secrets.** `.env` stays local; only `.env.example` (placeholders) is tracked.
- **Never put real customer PII into cloud-synced plaintext** (see P0-5).
- Build risky/paid integrations **behind capability flags** that stay off until the founder supplies verified infra.

---

## 3. Split of work: founder-only vs agent

**Owner: Founder (agent must NOT attempt — flag and wait):**
- **F1 — Take down / neutralise the live public site** `https://tideway-7.polsia.app/` (still advertising fabricated claims + a working booking form). This is the #1 real-world risk. Agent cannot access that deployment.
- Choose legal structure; obtain public liability insurance; open + verify the payment provider account (Stripe underwriting); recruit and vet real cleaners (DBS + references); sign off pricing, pilot postcodes, and the items in `../FOUNDER_DECISIONS.md`.
- Provide production infra: managed Postgres URL, SMTP/SMS credentials, object storage keys, map provider token, HTTPS origin. (Agent wires the code; founder supplies the accounts.)

**Owner: Agent (do these):** everything in §5 marked Owner: Agent — code fixes, tests, integrations behind flags, docs.

---

## 4. The one decision that sequences everything (RUNTIME FORK)

Before P1, the founder must record ONE choice in `../TIDEWAY_LAUNCH_CONTROL.md`:

- **Path A — Keep the NDJSON pilot as the product for now.** Park `src/marketplace/*` + migrations explicitly. Add payments/notifications onto `server.mjs`. Fastest to a first paid booking; won't scale to true automation.
- **Path B — Commit to the Postgres marketplace.** Add a driver, wire `runtime.mjs` into a server entrypoint, and stand up real DB tests. Slower, but this is the only path to a genuine automated "Uber" app.

Until this is chosen, the agent should do all of **P0** (both paths need it) and prepare P1 scaffolding behind flags.

---

## 5. Work queue

### P0 — Fix now (Owner: Agent unless noted; small, no infra required)

**P0-1 — Stop handing out the private tracker token on an email-only match (F6, High).**
- Files/anchors: `server.mjs`, the `/api/job-briefs` handler. Search the match line `requestTokenValid ? record.customerStatusToken === suppliedRequestToken : record.email === email` and the response field `customerStatusToken:` in that handler.
- Steps: require a valid `x-request-token` to submit a job brief; remove the `record.email === email` fallback path, OR keep an email path but **do not return `customerStatusToken`** in the response (the customer already has it from their tracker link).
- Acceptance: a job-brief POST with a valid `requestId` + `email` but no/invalid token no longer returns a `customerStatusToken` and cannot mutate the request.
- Validate: add an HTTP test asserting this; `npm test`.

**P0-2 — Harden CSRF: reject missing/blank Origin on mutations (F9, Medium).**
- Files/anchors: `server.mjs`, function `ensureSameOrigin` (currently `if (!origin) return;`).
- Steps: on state-changing routes (POST/PUT/PATCH/DELETE), treat a missing or blank `Origin` as a failure rather than a pass. Keep same-origin allow logic unchanged.
- Acceptance: a mutation request with no `Origin` header is rejected; same-origin requests still succeed.
- Validate: HTTP test for both cases; `npm test`.

**P0-3 — Fix mojibake in shipped UI text (F16, Medium).**
- Files/anchors: search the byte sequence `â€` across the repo. Known hits: `server.mjs` (2), `public/admin.html` (near the founder-decisions/readiness copy), `public/admin.js`, `public/cleaner-status.js`.
- Steps: replace corrupted em-dash/ellipsis with correct UTF-8 (`—`, `…`). Add a cheap guard to `npm run check` (e.g. a grep that fails if `â€` appears in `public/` or `server.mjs`).
- Acceptance: no `â€`/`Â` sequences remain; the guard passes.
- Validate: run the guard; visually confirm affected pages.

**P0-4 — Close the admin-shell gap and make loopback-admin explicit (F7, F12, Medium).**
- Files/anchors: `server.mjs`, `isAdminAuthorised`; the exact `/admin` gate (search `pathname === "/admin"`); `serveFile` route map (search `"/admin": "admin.html"`).
- Steps: (a) gate `/admin.html` the same way as `/admin`; (b) add `ensureSameOrigin` to admin GET endpoints; (c) add an env flag (e.g. `ADMIN_REQUIRE_KEY=true`) that requires `ADMIN_KEY` even on loopback, defaulting to current behaviour so local dev isn't broken.
- Acceptance: `/admin.html` returns 401 without authorisation; with `ADMIN_REQUIRE_KEY=true`, loopback requests without the key are denied.
- Validate: HTTP tests; `npm test`.

**P0-5 — Keep customer data out of cloud-synced plaintext + wire retention (F3, High). (Agent: code/guard. Founder: confirm data location + retention values.)**
- Files/anchors: `server.mjs`, `const dataDir = process.env.DATA_DIR ...`; retention config in `public/admin.html` / config validation.
- Steps: (a) add a startup warning (not a crash) if `dataDir` resolves inside a known cloud-sync path (e.g. contains `OneDrive`, `Dropbox`, `Google Drive`) and `DATA_DIR` was not explicitly set; (b) document the recommended non-synced `DATA_DIR` in `README.md` and `RECOVERY.md`; (c) ensure inactive-enquiry and completed-booking retention are required before real intake (surface them in launch readiness).
- Acceptance: starting with data in a synced folder prints a clear warning; docs state the correct location; retention fields are part of the readiness gate.
- Validate: boot with a synthetic synced path and confirm the warning; `npm run check`.

**P0-6 — Make backups trustworthy (F4, F13, Medium). (Agent: script. Founder: run it, store off-sync.)**
- Files/anchors: `tools/backup-data.ps1`; note `../Tideway-independent-source-2026-07-15.zip` is currently **corrupt/truncated**.
- Steps: add post-archive integrity verification (`Expand-Archive -WhatIf` / checksum) that fails loudly; write archives **outside** the OneDrive tree by default; add encryption (or clearly stop calling them "device-protected" in `RECOVERY.md`); regenerate a verified source archive.
- Acceptance: the script refuses to report success on a corrupt archive; the newest archive passes an integrity check; docs match reality.
- Validate: run the script; verify the emitted archive with `unzip -t` / `Expand-Archive`.

**P0-7 — Reconcile the launch docs (F14, Medium). (Owner: Agent, docs only.)**
- Files/anchors: `../TIDEWAY_LAUNCH_CONTROL.md` (scorecard vs progress log; stale module/test counts; missing £20 referral claim in the inventory).
- Steps: split the scorecard so each blocker names its property (Polsia site vs local app); refresh counts to the real totals (`find`); complete the unsupported-claims inventory.
- Acceptance: no line conflates the two sites; counts match the repo; the referral claim is listed.
- Validate: re-read against `find . -name '*.mjs' | wc -l` and `find tests -name '*.mjs' | wc -l`.

**P0-8 — Add end-to-end HTTP tests for the P0 auth fixes.**
- Steps: boot the server on a temp `DATA_DIR` (mirror `tests/smoke.mjs`) and assert P0-1 and P0-2 behaviours plus the P0-4 admin gate.
- Acceptance: tests fail before the fix, pass after; wired into `npm test`.

### P1 — Make it a real app that takes payments (bigger epics; needs the §4 decision + founder infra)

**E1 — Stand up the database + wire the runtime (F2, F8). Path B, or a slimmer version for Path A.**
- Steps: add and version-lock a maintained Postgres driver; construct a pool from `DATABASE_URL`; compose `createMarketplaceRuntime(pool)` into a real server entrypoint behind capability gates; apply every migration locked in `db/migration-lock.json` plus `runtime-role-grants.sql` / `worker-role-grants.sql` to a provisioned database.
- Acceptance: with infra present, `runtime` reports ready and authenticated routes respond; with infra absent, everything stays safely disabled (no partial login).
- Validate: real integration run (E2); `npm test`.
- Prepared source: the main server attachment, exact locked `pg` driver graph, pool loader/probe, deployment-adapter contract, truthful capability discovery, isolated dispatch and resource shutdown are complete behind `MARKETPLACE_ENABLED=false`. The E2 suite now passes locally on PostgreSQL 16.14. Still required: approved managed database credentials and a real deployment adapter/staging attachment.

**E2 — Real database integration tests (replaces string-match "DB tests") (F8).**
- Steps: provision approved PostgreSQL staging or ephemeral PostgreSQL 16 in CI; run the locked migrations and role grants; run `tools/postgres-verification-runner.mjs`; then exercise RLS (owner isolation), workflow-function authorization, concurrency, and the double-booking exclusion constraint against the live DB.
- Acceptance: RLS/concurrency/exclusion behaviours are asserted against real Postgres and pass.
- Prepared command: inject separate `DATABASE_INTEGRATION_OWNER_URL` and `DATABASE_INTEGRATION_APP_URL` secrets for a database ending `_tideway_test`, set the exact documented confirmation phrase, then run `node tools/postgres-integration-runner.mjs`. Do not use a production or customer-data database.
- Local evidence: the exact guarded command passed from a fresh PostgreSQL 16.14 database on 16 July 2026 and removed every reserved fixture. Repeat it in managed staging/CI before enabling the marketplace.

**E3 — Fix OAuth unverified-merge before enabling any OAuth route (F5, High).**
- Files/anchors: `db/migrations/004` (`resolve_social_identity`, the email-only link path); account creation in `005` (`email_verified_at` starts NULL).
- Steps: refuse to auto-link a provider into an account whose email was never verified; require step-up or credential reset on merge.
- Acceptance: the pre-registration takeover path is closed; a test proves an attacker-created unverified account cannot be linked/hijacked.
- Prepared source: takeover-safe Google resolution, Facebook pending-mailbox verification, password-account connection, exact-subject social-only step-up and lockout-safe provider removal pass adversarial source and local PostgreSQL tests. Removing a method cannot remove the final identity, requires proof through a method that remains and revokes all sessions. Activation still requires managed PostgreSQL staging, final HTTPS domain, approved SMTP, real provider credentials and Meta operational review/deletion evidence; all public controls remain hidden.

**E4 — Payments (ledger prepared; provider integration is not live). Owner: Agent builds; Founder supplies verified account + approves go-live.**
- Steps: integrate a marketplace payments provider (e.g. Stripe Connect): authorise/hold customer payment at booking, take the platform fee, pay out the cleaner, handle refunds and re-clean adjustments; verify events via signed webhooks; reconcile against the existing booking payment-evidence fields. Keep everything behind a `PAYMENTS_ENABLED` flag; test mode only until the founder approves.
- Acceptance: in test mode, a full charge → payout → refund cycle works and reconciles with booking records; no live charge occurs without the flag + approval.
- Validate: provider test-mode suite; webhook signature tests.
- Prepared source: the provider-neutral workflow and migrations 022-025 freeze authorization to the accepted booking total, hash retry keys, resume the one booking authorization after a browser restart, reserve role-bound capture/cancel/refund/transfer commands and keep payout destinations server-owned. Migration 036 and `/cleaner/payouts` add actor-bound, retry-stable Stripe-hosted test payout onboarding without collecting bank details. Migration 037 makes the exact frozen total visible before authorization. Migration 040 makes webhook reconciliation exactly once, blocks refund/transfer crossing and preserves verified transfer reversal. Migration 041 warns the Landlord within 24 hours when the exact authorization will not remain current at the scheduled start and links directly to the private payment step without changing money or booking state. Migration 042 withholds the dashboard payment action until the same five-day window and distinguishes not-yet-open from authorization-ready. Migration 043 adds one calm window-open update and retains one urgent 24-hour reminder only when authorization remains incomplete. Exact Stripe Node 22.1.1 supplies a test-key-only onboarding/manual-capture/refund/source-backed-transfer adapter pinned to API `2026-03-25.dahlia`; the raw signed-webhook route preserves exact bytes, rejects live/wrong-version events and ignores unrelated signed events. The mobile `/booking-payment` page authenticates the Landlord, displays that exact total, and loads Stripe Payment Element only after deliberate action. A participant-safe journey preflight and authoritative database trigger block job start when payment is missing, stale or mismatched. The earlier disposable PostgreSQL 16.14 run proves payout ownership and current-payment journey gating; migrations 037 and 039-043 still require guarded real execution. Composition stays default-off behind both flags. The approved platform, genuine connected-account onboarding and a real HTTPS test-mode cycle are still required; no provider was contacted.

**E5 — Notifications: email + SMS (F19).**
- Files/anchors: existing design in `src/marketplace/notification-*` and `email-notification-*`.
- Steps: verify the implemented internal SMTP transport against the approved provider; decide whether SMS is necessary for the first pilot; deploy the prepared worker process with `WORKER_EMAIL_ENABLED=true` only after SMTP staging; make status transitions notify both sides.
- Acceptance: booking/quote/arrival events send real (test-mode) notifications via a scheduled worker.

**E6 — Production config: HTTPS origin, secrets manager, private object storage for room media.**
- Steps: provision the configured private bucket for the implemented adapter; prove public-access denial, signed-header CORS, encryption, lifecycle cleanup, decode/EXIF behavior and the documented malware/threat decision; load secrets from a manager, not files; set `APP_ORIGIN` to the verified HTTPS origin; keep dispatch blocked until a non-local HTTPS origin is configured.
- Acceptance: media served from private storage via token-authorised URLs; no secret read from disk in production.

**E7 — Session purge + public rate limiting (F18). Source complete; staging evidence pending.**
- Steps: deploy the prepared supervisor under the restricted worker credential and the persistent shared limiter; verify all six continuous schedules, alerts and shared behavior against managed PostgreSQL and two application instances. The predecessor's five maintenance jobs passed once through the real local `tideway_worker` role; the migration 041 payment-readiness job still requires the guarded database proof.
- Acceptance: the sessions table stays bounded under real scheduled execution; public GET throttling is shared across instances.

### P2 — Make it truly automated "Uber" (after P1 is real and proven)

- **Automated real-time dispatch/matching** tuned for liquidity (auto-invite the best Cleaner, timeouts, requeue) — consent, profitable ranking, bounded attempts, leases, stale fallback and hardened invitation creation are source-complete; next prove two-worker concurrency, expiry/requeue and real delivery against PostgreSQL staging before scheduling it.
- **Live in-app GPS tracking in production** — real map provider, arrival notifications; document that mobile-web background tracking isn't guaranteed.
- **Two-way ratings surfaced end to end** — source complete in the participant booking flow; activation requires real database/RLS, moderation operations and two-account HTTPS evidence.
- **Dispute/refund/trust-&-safety flows** — participant case opening plus protected Administrator queue/resolution APIs are source-complete; the Administrator screen, evidence policy, refund linkage, fraud checks and re-clean handling remain open.
- **Observability** — structured logging, error monitoring, health/readiness probes, alerting, on-call.
- **Scale/perf** — replace the per-write full-integrity scan + global write lock with incremental checks (F11); shared/persistent rate limiter with a correct trusted-proxy hop (F10); plan NDJSON→DB migration if staying on files longer.
- **Maintainability** — split the 5,094-line `server.mjs` by route domain; extract the duplicated TTL/draft helpers; unify `?v=` import specifiers (F21).
- **Mobile** — harden PWA or ship native apps.

---

## 6. Definition of done per tier

- **Tier 0 (first real, manual booking):** honest public presence; legal entity + insurance + payment account exist (founder); the current app runs the ops manually; P0 complete.
- **Tier 1 (real app takes payments online):** §4 decision made; E1–E7 done; test-mode payments reconcile; real DB tests pass; P0 auth fixes shipped.
- **Tier 2 (automated "Uber"):** automated dispatch + live tracking + ratings + disputes + monitoring in production, handling real money reliably.

---

## 7. How to validate any change (run every time)

```
node --version            # must be >= 20 (engines)
npm run check             # syntax check across the tree
npm test                  # full suite incl. smoke; must be green
node server.mjs           # then GET /api/health should return {"ok":true,...healthy}
```

- Do not mark a task complete if `npm test` or `npm run check` fails.
- For auth/payment/DB changes, add a test that **fails before** and **passes after** the change.
- Keep commits small and message them by outcome (matches this repo's history style).

---

## 8. Timeline (founder-facing summary)

Assumes solo founder + AI codegen; a single experienced developer roughly halves the build tiers. Payments, insurance, and legal have irreducible lead times regardless of code speed.

- **First real (manual) booking — ~3–6 weeks.** Dominated by cleaner recruitment/vetting + insurance, not code. P0 is a few days of agent work.
- **Real app taking payments online — ~2–4 months.** E1–E7, with payments (E4) and accounts/DB (E1–E3) as the long poles.
- **Genuine automated "Uber for cleaning" — ~6–12+ months.** Gated by cleaner supply and trust more than by code.

**Guidance:** don't build P2 before Tier 0 proves customers will pay and cleaners will show up. Get ~5 cleaners and ~5 paying customers through the manual flow first; let that decide what to automate.

---

## 9. Task template (use for any new work you add here)

```
### <ID> — <short objective> (<finding ref if any>, <severity>)
- Owner: Agent | Founder
- Files/anchors: <paths + searchable function/string names>
- Why: <user/business impact in one line>
- Steps: <ordered, concrete>
- Acceptance: <observable pass condition>
- Validate: <exact commands / tests>
```
